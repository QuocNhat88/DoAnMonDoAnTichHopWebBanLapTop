-tài khoản admin : nnhat426@gmail.com . mật khẩu :123456

BoxchatAI : tạo một terminal rồi gõ 'source .venv/Scripts/activate && cd ai_server && uvicorn main:app --reload'


môi trường dev :

chức năng quên mật khẩu sửa thành như này trong file auth.service
const resetUrl = `${process.env.FRONTEND_URL || "http://localhost:5173"}/resetpassword/${resetToken}`; còn product xóa :5173

chức năng đăng nhập bằng gg : https://console.cloud.google.com/apis/credentials link này 